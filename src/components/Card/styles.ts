import styled from 'styled-components';

export const Wrapper = styled.div`
  position: relative;
  display: block;

  max-width: 36.9rem;
  width: 100%;
  margin: 2px;

  img {
    position: relative;
    border-radius: 1rem;
  }
`;
